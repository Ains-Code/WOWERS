{
  "name": "css-js-maker-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "WOWERS frontend AI code generation workspace and OpenRouter proxy server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && node --check script.js && node --test tests/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.33.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
